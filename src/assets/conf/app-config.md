# app-config.json sample

```json
{
    "app": {
        "port": 3000
    },
    "postgres": {
        "host": "localhost",
        "database": "postgres",
        "port": 5432,
        "user": "postgres",
        "password": "postgres",
        "keepAlive": true,
        "ssl": true,
        "certPath": "/app/certs/postgres.crt"
    }
}
```
